<%= form_with(model: registro_mx) do |form| %>
  <% if registro_mx.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(registro_mx.errors.count, "error") %> prohibited this registro_mx from being saved:</h2>

      <ul>
        <% registro_mx.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :redirige, style: "display: block" %>
    <%= form.text_field :redirige %>
  </div>

  <div>
    <%= form.label :prioridad, style: "display: block" %>
    <%= form.number_field :prioridad %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
